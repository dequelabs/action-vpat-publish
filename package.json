{
  "name": "action-vpat-publish",
  "version": "0.1.0",
  "description": "GitHub Action that publishes a VPAT report to a target repository",
  "main": "dist/index.js",
  "repository": "https://github.com/dequelabs/action-vpat-publish.git",
  "author": "Deque Systems, Inc.",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "ncc build --source-map src/index.ts",
    "prepare": "husky install"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.4.10",
    "@vercel/ncc": "^0.36.1",
    "husky": "^8.0.3",
    "typescript": "^5.1.6"
  }
}
